# Test data

Synthetic fixtures are built in code (`tests/conftest.py`) so tests stay offline and fast.

For optional full-pipeline comparison runs, place small FITS subsets here and mark tests with `@pytest.mark.slow`.
